# thingy_bridge — minimal dependency set
# (deliberately not pinned to workshop_bot's requirements; the bridge
# has a much smaller surface and we want startup + reinstall to be fast)

discord.py>=2.7.1,<3
httpx>=0.27.0,<1
APScheduler>=3.10.0,<4
python-dotenv>=1.2.2,<2
