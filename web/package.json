{
  "name": "thingy-web",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone Thingy web app — thin client of the Librarian API.",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
