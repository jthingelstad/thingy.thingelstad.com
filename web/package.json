{
  "name": "thingy-web",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone Thingy web app — thin client of the Librarian API.",
  "scripts": {
    "build": "vite build",
    "serve": "vite --host 0.0.0.0",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
